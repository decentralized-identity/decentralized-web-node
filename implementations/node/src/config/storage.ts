import appConfig from './app';

// var nano = require('nano')(`${appConfig.scheme}://${appConfig.hostName}:${appConfig.port}`);

// remove when initialized
const storage = { db: {} };

export default storage;
